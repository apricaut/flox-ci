# flox-ci

## Platform context

This repo is part of the **apricaut platform**. For the full system map + conventions (repo map, provisioning + AI site-generation pipeline, composition engine, cross-repo version coupling, deploy, prod DB/k8s access, git/gh/flox conventions), use the **`apricaut-platform`** skill — install once with `/plugin marketplace add apricaut/apricaut` then `/plugin install apricaut-platform@apricaut`.
